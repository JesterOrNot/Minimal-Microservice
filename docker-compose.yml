version: '3'

services:
  frontend:
    restart: on-failure
    build: src/frontend
    environment:
      ROCKET_ENV: production
    ports:
      - "8081:8081"
  backend:
    restart: on-failure
    build: src/frontend
    ports:
      - "8080:8080"
